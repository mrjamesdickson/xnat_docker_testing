apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: xnat-web
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: xnat.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: xnat-web
                port:
                  number: 8080
